# -*- eval: (add-hook 'after-save-hook (lambda () (chmod "~/.config/river/init" #o755)) nil t); eval: (add-hook 'after-save-hook 'org-babel-tangle nil t); -*-

#+TITLE: River Config
#+AUTHOR: Sean Shi
#+DESCRIPTION: the river config
#+STARTUP: fold
#+OPTIONS: toc:2
#+PROPERTY: header-args:sh :tangle ~/.config/river/init :results silent :session riversession :shebang #!/bin/sh

* preamble
** header
#+begin_src sh

# See the river(1), riverctl(1), and rivertile(1) man pages for complete
# documentation.
#+end_src
** starting apps
fixed dbus environment issue 2024-09-28, took soooooo long. This was causing issues with xdg-desktop-portals and some other things.
#+begin_src sh
dbus-update-activation-environment XDG_CURRENT_DESKTOP DISPLAY WAYLAND_DISPLAY & # set environment variables for flatpaks
waybar &
swaybg -i ~/pictures/draw/experiment.png &
dunst &
nm-applet &
pipewire &
swayidle -w &
#+end_src
** sensible settings
#+begin_src sh
# Fixes for some gtk apps looking bad
riverctl rule-add ssd

# Set keyboard repeat rate
riverctl set-repeat 50 300
riverctl focus-follows-cursor normal
riverctl set-cursor-warp on-focus-change
riverctl default-attach-mode below
#+end_src
** colors
#+begin_src sh
# Set background and border color
# riverctl background-color 0x000000
riverctl border-color-focused 0xa3b1b1
riverctl border-color-unfocused 0x283e45
riverctl border-color-urgent 0x700000
#+end_src
* Window management
Note: the "Super" modifier is also known as Logo, GUI, Windows, Mod4, etc.
** close
#+begin_src sh
# Super+Q to close the focused view
riverctl map normal Super Q close

# Super+Control+Shift+Q to exit river
riverctl map normal Super+Control+Shift Q exit

#+end_src
** monitors
#+begin_src sh
# Super+Period and Super+Comma to focus the next/previous output
riverctl map normal Super Period focus-output next
riverctl map normal Super Comma focus-output previous

# Super+Shift+{Period,Comma} to send the focused view to the next/previous output
riverctl map normal Super+Shift Period send-to-output next
riverctl map normal Super+Shift Comma send-to-output previous

#+end_src
** float
#+begin_src sh
# Super+Alt+{H,J,K,L} to move views
riverctl map normal Super+Alt H move left 100
riverctl map normal Super+Alt J move down 100
riverctl map normal Super+Alt K move up 100
riverctl map normal Super+Alt L move right 100

# Super+Alt+Control+{H,J,K,L} to snap views to screen edges
riverctl map normal Super+Alt+Control H snap left
riverctl map normal Super+Alt+Control J snap down
riverctl map normal Super+Alt+Control K snap up
riverctl map normal Super+Alt+Control L snap right

# Super+Alt+Shift+{H,J,K,L} to resize views
riverctl map normal Super+Alt+Shift H resize horizontal -100
riverctl map normal Super+Alt+Shift J resize vertical 100
riverctl map normal Super+Alt+Shift K resize vertical -100
riverctl map normal Super+Alt+Shift L resize horizontal 100

# Super + Left Mouse Button to move views
riverctl map-pointer normal Super BTN_LEFT move-view

# Super + Right Mouse Button to resize views
riverctl map-pointer normal Super BTN_RIGHT resize-view

# Super + Middle Mouse Button to toggle float
riverctl map-pointer normal Super BTN_MIDDLE toggle-float

# Super+Space to toggle float
riverctl map normal Super Space toggle-float

#+end_src
** tags
#+begin_src sh
for i in $(seq 1 9)
do
    tags=$((1 << ($i - 1)))

    # Super+[1-9] to focus tag [0-8]
    riverctl map normal Super $i set-focused-tags $tags

    # Super+Shift+[1-9] to tag focused view with tag [0-8]
    riverctl map normal Super+Shift $i set-view-tags $tags

    # Super+Control+[1-9] to toggle focus of tag [0-8]
    riverctl map normal Super+Control $i toggle-focused-tags $tags

    # Super+Shift+Control+[1-9] to toggle tag [0-8] of focused view
    riverctl map normal Super+Shift+Control $i toggle-view-tags $tags
done

# Super+0 to focus all tags
# Super+Shift+0 to tag focused view with all tags
all_tags=$(((1 << 32) - 1))
riverctl map normal Super 0 set-focused-tags $all_tags
riverctl map normal Super+Shift 0 set-view-tags $all_tags

#+end_src
** other
#+begin_src sh
# Super+F to toggle fullscreen
riverctl map normal Super F toggle-fullscreen

# Super+Shift+Return to bump the focused view to the top of the layout stack
riverctl map normal Super+Shift Return zoom

# Super+Tab to swap between 2 setups
riverctl map normal Super Tab focus-previous-tags
#+end_src
** layout generator
#+begin_src sh
# Super+J and Super+K to focus the next/previous view in the layout stack
riverctl map normal Super J focus-view next
riverctl map normal Super K focus-view previous

# Super+Shift+J and Super+Shift+K to swap the focused view with the next/previous
# view in the layout stack
riverctl map normal Super+Shift J swap next
riverctl map normal Super+Shift K swap previous
#+end_src

*** rivertile
#+begin_src sh :tangle no
# Super+H and Super+L to decrease/increase the main ratio of rivertile(1)
riverctl map normal Super H send-layout-cmd rivertile "main-ratio -0.05"
riverctl map normal Super L send-layout-cmd rivertile "main-ratio +0.05"

# Super+Shift+H and Super+Shift+L to increment/decrement the main count of rivertile(1)
riverctl map normal Super+Shift H send-layout-cmd rivertile "main-count +1"
riverctl map normal Super+Shift L send-layout-cmd rivertile "main-count -1"

# Set the default layout generator to be rivertile and start it.
# River will send the process group of the init executable SIGTERM on exit.
riverctl default-layout rivertile
riverctl default-attach-mode below
rivertile -view-padding 3 -outer-padding 3 &

# Super+{Up,Right,Down,Left} to change layout orientation
riverctl map normal Super Up    send-layout-cmd rivertile "main-location top"
riverctl map normal Super Right send-layout-cmd rivertile "main-location right"
riverctl map normal Super Down  send-layout-cmd rivertile "main-location bottom"
riverctl map normal Super Left  send-layout-cmd rivertile "main-location left"
#+end_src
*** rivercarro
#+begin_src sh
mod=Super
# Mod+H and Mod+L to decrease/increase the main ratio of rivercarro
riverctl map normal $mod H send-layout-cmd rivercarro "main-ratio -0.05"
riverctl map normal $mod L send-layout-cmd rivercarro "main-ratio +0.05"

# Mod+Shift+H and Mod+Shift+L to increment/decrement the main count of rivercarro
riverctl map normal $mod+Shift H send-layout-cmd rivercarro "main-count +1"
riverctl map normal $mod+Shift L send-layout-cmd rivercarro "main-count -1"

# Mod+{Up,Right,Down,Left} to change layout orientation
riverctl map normal $mod Up    send-layout-cmd rivercarro "main-location top"
riverctl map normal $mod Right send-layout-cmd rivercarro "main-location right"
riverctl map normal $mod Down  send-layout-cmd rivercarro "main-location bottom"
riverctl map normal $mod Left  send-layout-cmd rivercarro "main-location left"
# And for monocle
riverctl map normal $mod M     send-layout-cmd rivercarro "main-location monocle"
# Cycle through layout
riverctl map normal $mod W     send-layout-cmd rivercarro "main-location-cycle left,monocle"

# Add others rivercarro's commands the same way with the keybinds you'd like.
# e.g.
# riverctl map normal $mod <keys> send-layout-cmd rivercarro "inner-gaps -1"
# riverctl map normal $mod <keys> send-layout-cmd rivercarro "inner-gaps +1"
# riverctl map normal $mod <keys> send-layout-cmd rivercarro "outer-gaps -1"
# riverctl map normal $mod <keys> send-layout-cmd rivercarro "outer-gaps +1"
# riverctl map normal $mod <keys> send-layout-cmd rivercarro "width-ratio -0.1"
# riverctl map normal $mod <keys> send-layout-cmd rivercarro "width-ratio +0.1"

# Set the default layout generator to be rivercarro and start it.
# River will send the process group of the init executable SIGTERM on exit.
riverctl default-layout rivercarro
rivercarro -per-tag &
#+end_src
** scratchpad/sticky
Taken from
https://codeberg.org/river/wiki/src/branch/master/pages/Scratchpad-and-Sticky.md
#+begin_src sh
# The scratchpad will live on an unused tag. Which tags are used depends on your
# config, but rivers default uses the first 9 tags.
scratch_tag=$((1 << 20 ))

# Toggle the scratchpad with Super+Minus
riverctl map normal Super Minus toggle-focused-tags ${scratch_tag}

# Send windows to the scratchpad with Super+Shift+Minus
riverctl map normal Super+Shift Minus set-view-tags ${scratch_tag}

# Set spawn tagmask to ensure new windows don't have the scratchpad tag unless
# explicitly set.
all_tags=$(((1 << 32) - 1))
all_but_scratch_tag=$(( $all_tags ^ $scratch_tag ))
riverctl spawn-tagmask ${all_but_scratch_tag}

sticky_tag=$((1 << 31))
all_but_sticky_tag=$(( $all_tags ^ $sticky_tag ))

riverctl map normal Super S toggle-view-tags $sticky_tag
riverctl spawn-tagmask ${all_but_sticky_tag}

# modify the normal keybind to always select the sticky tag
for i in $(seq 1 9)
do
    tags=$((1 << ($i - 1)))
    # Super+[1-9] to focus tag [0-8]
    riverctl map normal Super $i set-focused-tags $(($sticky_tag + $tags))
done

#+end_src
* Passthrough(disable bindings)
#+begin_src sh
# Declare a passthrough mode. This mode has only a single mapping to return to
# normal mode. This makes it useful for testing a nested wayland compositor
riverctl declare-mode passthrough

# Super+F11 to enter passthrough mode
riverctl map normal Super F11 enter-mode passthrough

# Super+F11 to return to normal mode
riverctl map passthrough Super F11 enter-mode normal
#+end_src
* keybindings
** Media Keys
#+begin_src sh
# Various media key mapping examples for both normal and locked mode which do
# not have a modifier
for mode in normal locked
do
    # Eject the optical drive (well if you still have one that is)
    riverctl map $mode None XF86Eject spawn 'eject -T'

    # Control pulse audio volume with pamixer (https://github.com/cdemoulins/pamixer)
    # riverctl map $mode None XF86AudioRaiseVolume  spawn 'pamixer -i 5'
    # riverctl map $mode None XF86AudioLowerVolume  spawn 'pamixer -d 5'
    # riverctl map $mode None XF86AudioMute         spawn 'pamixer --toggle-mute'
    riverctl map $mode None XF86AudioRaiseVolume  spawn 'amixer set Master 5%+'
    riverctl map $mode None XF86AudioLowerVolume  spawn 'amixer set Master 5%-'
    riverctl map $mode None XF86AudioMute         spawn 'amixer set Master toggle'
    riverctl map $mode None XF86AudioMicMute      spawn 'amixer set Capture toggle'

    # Control MPRIS aware media players with playerctl (https://github.com/altdesktop/playerctl)
    riverctl map $mode None XF86AudioMedia spawn 'playerctl play-pause'
    riverctl map $mode None XF86AudioPlay  spawn 'playerctl play-pause'
    riverctl map $mode None XF86AudioPrev  spawn 'playerctl previous'
    riverctl map $mode None XF86AudioNext  spawn 'playerctl next'

    # Control screen backlight brightness with brightnessctl (https://github.com/Hummer12007/brightnessctl)
    riverctl map $mode None XF86MonBrightnessUp   spawn 'brightnessctl set +5%'
    riverctl map $mode None XF86MonBrightnessDown spawn 'brightnessctl set 5%-'
    # These do not work for some reason
    # riverctl map $mode Super XF86MonBrightnessUp   spawn 'brightnessctl set 100%'
    # riverctl map $mode Super XF86MonBrightnessDown spawn 'brightnessctl set 0%'
    riverctl map $mode Super F8   spawn 'brightnessctl set 0%'
    riverctl map $mode Super F9   spawn 'brightnessctl set 100%'
    riverctl map $mode Super F4   spawn 'loginctl hibernate'
done
#+end_src
*** Lock on sleep key and lock key
#+begin_src sh
riverctl map normal None XF86Sleep       spawn 'swaylock'
riverctl map normal None XF86ScreenSaver spawn 'swaylock'
#+end_src
*** screenshots
#+begin_src sh
riverctl map normal Super Print       spawn 'grim; notify-send "captured screenshot"'
riverctl map normal Control Print     spawn 'grim -g "$(slurp)"; notify-send "captured screenshot"'
riverctl map normal Alt+Control Print spawn 'grim -g "$(slurp -p)" -t ppm - | magick - -format '%[pixel:p{0,0}]' txt:- | tail -n 1 | cut -d " " -f 4 | wl-copy ; notify-send "copied color $(wl-paste) to clipboard"'
#+end_src

** hotkeys
#+begin_src sh
# Super+Return to start an instance of foot (https://codeberg.org/dnkl/foot)
riverctl map normal Super Return spawn foot
riverctl map normal Super P spawn fuzzel
riverctl map normal Super+Control P spawn "fuzzel --list-executables-in-path"
riverctl map normal Super E spawn "emacsclient --alternate-editor='' --create-frame"
riverctl map normal Super X spawn "fuzzpkill"
riverctl map normal Super+Shift X spawn "fuzzkill"
# copy command to set currently focused window to float to clipboard
riverctl map normal Super+Control+Shift Space spawn "lswt | awk -F \"  +\" 'NR != 1 && /^..a./ {print \"riverctl rule-add -app-id \" \$2 \" -title \" \$3 \" float\"}' | wl-copy; notify-send 'command copied'"
# I need those clickers
riverctl map normal Super+Control+Shift C spawn "xdg-open 'https://shibboleth-sp.reef-education.com/Shibboleth.sso/Login?entityID=https%3A%2F%2Ffedauth.colorado.edu%2Fidp%2Fshibboleth&target=https%3A%2F%2Fshibboleth-sp.reef-education.com%2Fv1%2Fusers%2Fcheck-federated-redirect%3FinstitutionId%3Db6454bb5-fd0d-473d-95b9-760440882390'"

# riverctl map normal Super V spawn "nmcli connection up 'UCB VPN rc' && notify-send 'Connected to UCB VPN rc'"
riverctl map normal Super V spawn "if nmcli connection show 'UCB VPN rc' | grep GENERAL.STATE > /dev/null; then nmcli connection down 'UCB VPN rc' && notify-send 'Disconnected from UCB VPN rc'; else nmcli connection up 'UCB VPN rc' && notify-send 'Connected to UCB VPN rc'; fi"
#+end_src
* rules
use =lswt= to get app-id's and names
** Floating windows
#+begin_src sh
riverctl rule-add -app-id nm-openconnect-auth-dialog float
riverctl rule-add -app-id nm-openconnect-auth-dialog position 600 20
riverctl rule-add -app-id nm-connection-editor float
riverctl rule-add -app-id zoom -title zoom float
riverctl rule-add -app-id zoom -title "Zoom Workplace" float
riverctl rule-add -app-id zoom -title "Zoom Workplace" position 0 20
riverctl rule-add -app-id org.inkscape.Inkscape -title "New From Template" float
riverctl rule-add -app-id Firefox -title "Picture-in-Picture" float
riverctl rule-add -app-id Gimp-2.10 -title "Quit GIMP" float
#+end_src
** default tags
1. terminals
2. web browser + password manager in monocle
3. emacs session for notes and assignments
4. actually productive apps
5. "productivity" apps on in monocle
#+begin_src sh
riverctl rule-add -app-id org.keepassxc.KeePassXC -title "Passwords.kdbx [Locked] - KeePassXC" tags $((1 << (2-1)))
riverctl rule-add -app-id Firefox tags $((1 << (2-1)))

riverctl rule-add -app-id Slack tags $((1 << (5-1)))
riverctl rule-add -app-id zoom -title Meeting tags $((1 << (5-1)))
#+end_src
